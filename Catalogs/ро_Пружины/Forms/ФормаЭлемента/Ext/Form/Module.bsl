
&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	//Если СокрдП(Объект.ДопИнфо)="" ТОгда
	//	Объект.Наименование = ОБъект.КОд;
	//ИНаче
	//	Объект.Наименование = ОБъект.Код+" "+СокрЛП(Объект.ДопИнфо);
	//КонецЕсли;
КонецПроцедуры
