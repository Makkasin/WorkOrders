&НаСервере

Процедура ЗаполнитьФИОнаСервере()
	МассивДанных =	СтрРазделить(Объект.Наименование," ");
Объект.Фамилия = МассивДанных[0];
Объект.Имя = МассивДанных[1];
Объект.Отчество = МассивДанных[2];
Конецпроцедуры

&НаКлиенте
Процедура НаименованиеПриИзменении(Элемент) 
	ЗаполнитьФИОнаСервере();
КонецПроцедуры
