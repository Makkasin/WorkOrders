
&НаСервереБезКонтекста
Процедура ПереписатьВсеНаСервере()
	
	Наб = РегистрыСведений.Прайс.СоздатьНаборЗаписей();
	Наб.Прочитать();
	Для каждого ТекущийОбъект из Наб Цикл
		ТекущийОбъект.ОписаниеПараметров = РегистрыСведений.Прайс.ПолучитьОписаниеПараметров(ТекущийОбъект);
	КонецЦикла;
	
	Наб.Записать();
	
КонецПроцедуры

&НаКлиенте
Процедура ПереписатьВсе(Команда)
	ПереписатьВсеНаСервере();
КонецПроцедуры
